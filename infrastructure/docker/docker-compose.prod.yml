version: "3.9"

x-service-defaults: &service-defaults
  restart: unless-stopped
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"

x-api-healthcheck: &api-healthcheck
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 40s

services:
  # ─── Infrastructure ─────────────────────────────────────────────────────────

  postgres:
    <<: *service-defaults
    image: postgres:16-alpine
    container_name: elevatedpos_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init/postgres:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - elevatedpos_internal

  redis:
    <<: *service-defaults
    image: redis:7-alpine
    container_name: elevatedpos_redis
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--no-auth-warning", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - elevatedpos_internal

  typesense:
    <<: *service-defaults
    image: typesense/typesense:26.0
    container_name: elevatedpos_typesense
    environment:
      TYPESENSE_DATA_DIR: /data
      TYPESENSE_API_KEY: ${TYPESENSE_API_KEY}
    volumes:
      - typesense_data:/data
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8108/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - elevatedpos_internal

  clickhouse:
    <<: *service-defaults
    image: clickhouse/clickhouse-server:24-alpine
    container_name: elevatedpos_clickhouse
    environment:
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-elevatedpos_analytics}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD}
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8123/ping"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - elevatedpos_internal

  # ─── API Services ────────────────────────────────────────────────────────────

  auth:
    <<: *service-defaults
    image: elevatedpos-auth:latest
    container_name: elevatedpos_auth
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PORT: 4001
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4001/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  catalog:
    <<: *service-defaults
    image: elevatedpos-catalog:latest
    container_name: elevatedpos_catalog
    depends_on:
      postgres:
        condition: service_healthy
      typesense:
        condition: service_healthy
    environment:
      PORT: 4002
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      TYPESENSE_HOST: typesense
      TYPESENSE_API_KEY: ${TYPESENSE_API_KEY}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4002/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  inventory:
    <<: *service-defaults
    image: elevatedpos-inventory:latest
    container_name: elevatedpos_inventory
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 4003
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4003/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  orders:
    <<: *service-defaults
    image: elevatedpos-orders:latest
    container_name: elevatedpos_orders
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PORT: 4004
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4004/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  payments:
    <<: *service-defaults
    image: elevatedpos-payments:latest
    container_name: elevatedpos_payments
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 4005
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4005/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  customers:
    <<: *service-defaults
    image: elevatedpos-customers:latest
    container_name: elevatedpos_customers
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 4006
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4006/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  loyalty:
    <<: *service-defaults
    image: elevatedpos-loyalty:latest
    container_name: elevatedpos_loyalty
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 4007
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4007/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  campaigns:
    <<: *service-defaults
    image: elevatedpos-campaigns:latest
    container_name: elevatedpos_campaigns
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PORT: 4008
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4008/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  notifications:
    <<: *service-defaults
    image: elevatedpos-notifications:latest
    container_name: elevatedpos_notifications
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PORT: 4009
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4009/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  integrations:
    <<: *service-defaults
    image: elevatedpos-integrations:latest
    container_name: elevatedpos_integrations
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 4010
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4010/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  automations:
    <<: *service-defaults
    image: elevatedpos-automations:latest
    container_name: elevatedpos_automations
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PORT: 4011
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4011/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  ai:
    <<: *service-defaults
    image: elevatedpos-ai:latest
    container_name: elevatedpos_ai
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 4012
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4012/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  franchise:
    <<: *service-defaults
    image: elevatedpos-franchise:latest
    container_name: elevatedpos_franchise
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 4013
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4013/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  reporting:
    <<: *service-defaults
    image: elevatedpos-reporting:latest
    container_name: elevatedpos_reporting
    depends_on:
      postgres:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    environment:
      PORT: 4014
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_SECRET: ${INTERNAL_SECRET}
      CLICKHOUSE_URL: http://${CLICKHOUSE_USER}:${CLICKHOUSE_PASSWORD}@clickhouse:8123
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-elevatedpos_analytics}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4014/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  webhooks:
    <<: *service-defaults
    image: elevatedpos-webhooks:latest
    container_name: elevatedpos_webhooks
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PORT: 4015
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_SECRET: ${INTERNAL_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4015/health"]
      <<: *api-healthcheck
    networks:
      - elevatedpos_internal

  # ─── Frontend Apps ───────────────────────────────────────────────────────────

  web-backoffice:
    <<: *service-defaults
    image: elevatedpos-web-backoffice:latest
    container_name: elevatedpos_web_backoffice
    depends_on:
      auth:
        condition: service_healthy
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      NEXTAUTH_URL: ${NEXTAUTH_URL}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - elevatedpos_internal

  kds-display:
    <<: *service-defaults
    image: elevatedpos-kds-display:latest
    container_name: elevatedpos_kds_display
    depends_on:
      orders:
        condition: service_healthy
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      # Server-side env vars for the bump proxy route
      ORDERS_SERVICE_URL: http://orders:4004
      INTERNAL_SECRET: ${INTERNAL_SECRET}
      # Client-side WebSocket URL for KDS stream (browser → nginx → orders)
      NEXT_PUBLIC_ORDERS_API_URL: ${NEXT_PUBLIC_ORDERS_API_URL:-}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - elevatedpos_internal

  # ─── Nginx Reverse Proxy ─────────────────────────────────────────────────────

  nginx:
    <<: *service-defaults
    image: nginx:1.27-alpine
    container_name: elevatedpos_nginx
    depends_on:
      - web-backoffice
      - auth
      - catalog
      - inventory
      - orders
      - payments
      - customers
      - loyalty
      - campaigns
      - notifications
      - integrations
      - automations
      - ai
      - franchise
      - reporting
      - webhooks
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - elevatedpos_internal
      - elevatedpos_external

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  typesense_data:
    driver: local
  clickhouse_data:
    driver: local

networks:
  elevatedpos_internal:
    name: elevatedpos_prod_internal
    internal: true
  elevatedpos_external:
    name: elevatedpos_prod_external
