apiVersion: apps/v1
kind: Deployment
metadata:
  name: automations
  namespace: nexus
  labels:
    app: automations
    app.kubernetes.io/part-of: nexus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: automations
  template:
    metadata:
      labels:
        app: automations
    spec:
      serviceAccountName: nexus-service
      containers:
        - name: automations
          image: ACCOUNT_ID.dkr.ecr.ap-southeast-2.amazonaws.com/nexus/automations:latest
          ports:
            - containerPort: 4011
          envFrom:
            - configMapRef:
                name: nexus-config
            - secretRef:
                name: nexus-secrets
          env:
            - name: PORT
              value: "4011"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 4011
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4011
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: automations
  namespace: nexus
spec:
  selector:
    app: automations
  ports:
    - port: 4011
      targetPort: 4011
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: automations
  namespace: nexus
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: automations
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
