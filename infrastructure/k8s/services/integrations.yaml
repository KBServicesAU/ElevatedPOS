apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrations
  namespace: nexus
  labels:
    app: integrations
    app.kubernetes.io/part-of: nexus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: integrations
  template:
    metadata:
      labels:
        app: integrations
    spec:
      serviceAccountName: nexus-service
      containers:
        - name: integrations
          image: ACCOUNT_ID.dkr.ecr.ap-southeast-2.amazonaws.com/nexus/integrations:latest
          ports:
            - containerPort: 4010
          envFrom:
            - configMapRef:
                name: nexus-config
            - secretRef:
                name: nexus-secrets
          env:
            - name: PORT
              value: "4010"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: integrations
  namespace: nexus
spec:
  selector:
    app: integrations
  ports:
    - port: 4010
      targetPort: 4010
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: integrations
  namespace: nexus
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: integrations
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
