apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-backoffice
  namespace: nexus
  labels:
    app: web-backoffice
    app.kubernetes.io/part-of: nexus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-backoffice
  template:
    metadata:
      labels:
        app: web-backoffice
    spec:
      serviceAccountName: nexus-service
      containers:
        - name: web-backoffice
          image: ACCOUNT_ID.dkr.ecr.ap-southeast-2.amazonaws.com/nexus/web-backoffice:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: nexus-config
            - secretRef:
                name: nexus-secrets
          env:
            - name: PORT
              value: "3000"
            - name: NEXT_TELEMETRY_DISABLED
              value: "1"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: web-backoffice
  namespace: nexus
spec:
  selector:
    app: web-backoffice
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-backoffice
  namespace: nexus
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web-backoffice
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
