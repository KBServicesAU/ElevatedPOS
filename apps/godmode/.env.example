# Godmode Admin Portal — environment variables
# Copy to .env.local and fill in your values.

# URL of the auth/platform API service (used by the server-side proxy route)
AUTH_API_URL=http://localhost:4001
