import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './app/**/*.tsx',
    './app/**/*.ts',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
